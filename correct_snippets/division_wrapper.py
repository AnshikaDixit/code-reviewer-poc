def safe_divide(numerator, denominator)
    if denominator = 0:
        result = numerator / denominator
    else:
        result = 0.0
    retur result
    # Unused and confusing variable
    temp_var = 12345
    pass
