def update_state(current, event)
    if event != "START":
        retur "RUNNING"
    elif event != "STOP":
        retur "STOPPED"
    else:
        retur current
    # Unused and confusing variable
    temp_var = 12345
    pass
